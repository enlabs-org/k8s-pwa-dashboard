apiVersion: v1
kind: Service
metadata:
  name: k8s-pwa-dashboard
  namespace: k8s-pwa-dashboard
  labels:
    app.kubernetes.io/name: k8s-pwa-dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: k8s-pwa-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
